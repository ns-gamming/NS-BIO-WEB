
import { ArrowLeft } from "lucide-react";
import { useLocation } from "wouter";

export default function BackButton() {
  // Back button is now in navigation, so this component is hidden
  // Keeping it in the codebase for potential future use
  return null;
}
